query CaseTypes($take: Int, $skip: Int) {
  caseTypes(take: $take, skip: $skip) {
    items {
      id
      name
      slug
      description
      color
      icon
      order
      active
    }
    pageInfo {
      totalCount
      hasNextPage
      hasPreviousPage
    }
  }
}
