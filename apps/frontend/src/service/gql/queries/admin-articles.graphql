# Query for admin to get all articles (including drafts and archived)
query AdminArticles($take: Int, $skip: Int, $search: String, $status: String) {
  articles(take: $take, skip: $skip, search: $search, status: $status) {
    items {
      id
      title
      slug
      excerpt
      status
      publishedAt
      createdAt
      updatedAt
      views
      author {
        id
        name
        email
      }
      categories {
        id
        name
      }
    }
    pageInfo {
      totalCount
      hasNextPage
      hasPreviousPage
    }
  }
}
