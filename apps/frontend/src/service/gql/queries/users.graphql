# Query to get a list of users (admin only)
query users($take: Int, $skip: Int, $filter: UserFilter) {
  users(take: $take, skip: $skip, filter: $filter) {
    items {
      id
      email
      name
      image
      role
      createdAt
      updatedAt
    }
    pageInfo {
      totalCount
      hasNextPage
      hasPreviousPage
    }
  }
}

query me {
  me {
    id
    name
    email
    emailVerified
    role
    image
    createdAt
    updatedAt
  }
}
